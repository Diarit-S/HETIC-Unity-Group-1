using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpeedIncrementation : MonoBehaviour
{

    // The script for incrementing speed while advancing in score gonna take place here
    // Don't forget to set an incrementation limit

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
